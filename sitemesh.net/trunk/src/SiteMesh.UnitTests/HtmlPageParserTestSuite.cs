using System.IO;

using NUnit.Framework;

namespace SiteMesh.Test.UnitTest
{

	public class HtmlPageParserTestSuite
	{
/*
		
		[Suite]
		public static TestSuite Suite
		{
			get
			{
				TestSuite tSuite = new TestSuite("HtmlPageParserTestSuite");
				string[] files = Directory.GetFiles(@"..\..\parser-tests");

				foreach (string fname in files)
				{
					FileInfo fInfo = new FileInfo(fname);
					if (fInfo.Name.StartsWith("test"))
					{
						HtmlPageParserTest fixture = new HtmlPageParserTest();
						fixture.Context = fname;
						tSuite.Add(fixture);
					}
				}
				return tSuite;
			}
		}
*/
	}
}